<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View" />

        <variable
            name="adapter"
            type="com.moviesdb.moviesdbmvvm.activity.adapter.MainActivityRecyclerViewAdapter"/>

        <variable
            name="viewModel"
            type="com.moviesdb.moviesdbmvvm.viewmodel.MainActivityViewModel" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ProgressBar
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:indeterminate="true"
            android:visibility="@{viewModel.status.initialProgressBarVisible ? View.VISIBLE: View.GONE}"
            android:layout_centerInParent="true"
            />
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="@{viewModel.errorMessage}"
            android:visibility="@{viewModel.status.errorMessageVisible ? View.VISIBLE: View.GONE}"/>
        <android.support.v7.widget.RecyclerView
            android:id="@+id/mainRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:adapter="@{adapter}"
            tools:data="@{viewModel.viewModelsToShow}"
            android:visibility="@{viewModel.status.recyclerViewVisible ? View.VISIBLE: View.GONE}"/>
    </RelativeLayout>

</layout>